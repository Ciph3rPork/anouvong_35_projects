import 'package:flutter/material.dart';

class anouvong extends StatelessWidget {
  const anouvong({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Text('hey ninjas'),
    );
  }
}
